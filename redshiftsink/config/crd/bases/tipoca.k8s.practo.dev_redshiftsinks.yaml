
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: redshiftsinks.tipoca.k8s.practo.dev
spec:
  group: tipoca.k8s.practo.dev
  names:
    kind: RedshiftSink
    listKind: RedshiftSinkList
    plural: redshiftsinks
    shortNames:
    - rsk
    - rsks
    singular: redshiftsink
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: RedshiftSink is the Schema for the redshiftsinks API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: RedshiftSinkSpec defines the desired state of RedshiftSink
          properties:
            batcher:
              description: RedshiftBatcherSpec defines the desired state of RedshiftBatcher
              properties:
                mask:
                  description: Mask when turned on enables masking of the data. Defaults
                    to false
                  type: boolean
                maskFile:
                  description: MaskFile to use to apply mask configurations
                  type: string
                maxConcurrency:
                  type: integer
                maxProcessingTime:
                  description: MaxProcessingTime is the sarama configuration MaxProcessingTime
                    It is the max time in milliseconds required to consume one message.
                    Defaults to 1000ms
                  format: int32
                  type: integer
                maxSize:
                  description: 'Deprecated all of the below spec in favour of SinkGroup
                    #167'
                  type: integer
                maxWaitSeconds:
                  type: integer
                podTemplate:
                  description: PodTemplate describes the pods that will be created.
                    if this is not specifed, a default pod template is created
                  properties:
                    image:
                      description: Image for the underlying pod
                      type: string
                    resources:
                      description: Resources is for configuring the compute resources
                        required
                      properties:
                        limits:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: 'Limits describes the maximum amount of compute
                            resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                          type: object
                        requests:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: 'Requests describes the minimum amount of compute
                            resources required. If Requests is omitted for a container,
                            it defaults to Limits if that is explicitly specified,
                            otherwise to an implementation-defined value. More info:
                            https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                          type: object
                      type: object
                    tolerations:
                      description: Toleartions the underlying pods should have
                      items:
                        description: The pod this Toleration is attached to tolerates
                          any taint that matches the triple <key,value,effect> using
                          the matching operator <operator>.
                        properties:
                          effect:
                            description: Effect indicates the taint effect to match.
                              Empty means match all taint effects. When specified,
                              allowed values are NoSchedule, PreferNoSchedule and
                              NoExecute.
                            type: string
                          key:
                            description: Key is the taint key that the toleration
                              applies to. Empty means match all taint keys. If the
                              key is empty, operator must be Exists; this combination
                              means to match all values and all keys.
                            type: string
                          operator:
                            description: Operator represents a key's relationship
                              to the value. Valid operators are Exists and Equal.
                              Defaults to Equal. Exists is equivalent to wildcard
                              for value, so that a pod can tolerate all taints of
                              a particular category.
                            type: string
                          tolerationSeconds:
                            description: TolerationSeconds represents the period of
                              time the toleration (which must be of effect NoExecute,
                              otherwise this field is ignored) tolerates the taint.
                              By default, it is not set, which means tolerate the
                              taint forever (do not evict). Zero and negative values
                              will be treated as 0 (evict immediately) by the system.
                            format: int64
                            type: integer
                          value:
                            description: Value is the taint value the toleration matches
                              to. If the operator is Exists, the value should be empty,
                              otherwise just a regular string.
                            type: string
                        type: object
                      type: array
                  type: object
                sinkGroup:
                  description: SinkGroup contains the specification for main, reload
                    and reloadDupe sinkgroups. Operator uses 3 groups to perform Redshiftsink.
                    The topics which have never been released is part of Reload SinkGroup,
                    the topics which gets released moves to the Main SinkGroup. ReloadDupe
                    SinkGroup is used to give realtime upaates to the topics which
                    are reloading. Defaults are there for all sinkGroups if none is
                    specifed.
                  properties:
                    all:
                      description: All specifies a common specification for all SinkGroups
                      properties:
                        deploymentUnit:
                          description: 'DeploymentUnit(pod) is the unit of deployment
                            for the batcher or the loader. Using this user can specify
                            the amount of resources needed to run them as one unit.
                            Operator calculates the total units based on the total
                            number of topics and this unit spec. This majorly solves
                            the scaling issues described in #167.'
                          properties:
                            podTemplate:
                              description: PodTemplate describes the pod specification
                                for the unit.
                              properties:
                                image:
                                  description: Image for the underlying pod
                                  type: string
                                resources:
                                  description: Resources is for configuring the compute
                                    resources required
                                  properties:
                                    limits:
                                      additionalProperties:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      description: 'Limits describes the maximum amount
                                        of compute resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                                      type: object
                                    requests:
                                      additionalProperties:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      description: 'Requests describes the minimum
                                        amount of compute resources required. If Requests
                                        is omitted for a container, it defaults to
                                        Limits if that is explicitly specified, otherwise
                                        to an implementation-defined value. More info:
                                        https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                                      type: object
                                  type: object
                                tolerations:
                                  description: Toleartions the underlying pods should
                                    have
                                  items:
                                    description: The pod this Toleration is attached
                                      to tolerates any taint that matches the triple
                                      <key,value,effect> using the matching operator
                                      <operator>.
                                    properties:
                                      effect:
                                        description: Effect indicates the taint effect
                                          to match. Empty means match all taint effects.
                                          When specified, allowed values are NoSchedule,
                                          PreferNoSchedule and NoExecute.
                                        type: string
                                      key:
                                        description: Key is the taint key that the
                                          toleration applies to. Empty means match
                                          all taint keys. If the key is empty, operator
                                          must be Exists; this combination means to
                                          match all values and all keys.
                                        type: string
                                      operator:
                                        description: Operator represents a key's relationship
                                          to the value. Valid operators are Exists
                                          and Equal. Defaults to Equal. Exists is
                                          equivalent to wildcard for value, so that
                                          a pod can tolerate all taints of a particular
                                          category.
                                        type: string
                                      tolerationSeconds:
                                        description: TolerationSeconds represents
                                          the period of time the toleration (which
                                          must be of effect NoExecute, otherwise this
                                          field is ignored) tolerates the taint. By
                                          default, it is not set, which means tolerate
                                          the taint forever (do not evict). Zero and
                                          negative values will be treated as 0 (evict
                                          immediately) by the system.
                                        format: int64
                                        type: integer
                                      value:
                                        description: Value is the taint value the
                                          toleration matches to. If the operator is
                                          Exists, the value should be empty, otherwise
                                          just a regular string.
                                        type: string
                                    type: object
                                  type: array
                              type: object
                          type: object
                        maxConcurrency:
                          description: MaxConcurrency is the maximum no, of batch
                            processors to run concurrently. This spec is useful when
                            the sink group pod operates in asynchronous mode. Loader
                            pods does not needed this as they are synchronous.
                          type: integer
                        maxProcessingTime:
                          description: MaxProcessingTime is the max time in ms required
                            to consume one message. Defaults for the batcher is 180000ms
                            and loader is 600000ms.
                          format: int32
                          type: integer
                        maxReloadingUnits:
                          description: MaxReloadingUnits is the maximum number of
                            units(pods) that can be launched based on the DeploymentUnit
                            specification. Only valid for Reloading SinkGroup.
                          format: int32
                          type: integer
                        maxSizePerBatch:
                          anyOf:
                          - type: integer
                          - type: string
                          description: 'MaxSizePerBatch is the maximum size of the
                            batch in bytes, Ki, Mi, Gi Example values: 1000, 1Ki,
                            100Mi, 1Gi 1000 is 1000 bytes, 1Ki is 1 Killo byte, 100Mi
                            is 100 mega bytes, 1Gi is 1 Giga bytes'
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        maxWaitSeconds:
                          description: MaxWaitSeconds is the maximum time to wait
                            before making a batch, make a batch if MaxSizePerBatch
                            is not hit during MaxWaitSeconds.
                          type: integer
                      type: object
                    main:
                      description: Main specifies the MainSinkGroup specification,
                        overwrites All
                      properties:
                        deploymentUnit:
                          description: 'DeploymentUnit(pod) is the unit of deployment
                            for the batcher or the loader. Using this user can specify
                            the amount of resources needed to run them as one unit.
                            Operator calculates the total units based on the total
                            number of topics and this unit spec. This majorly solves
                            the scaling issues described in #167.'
                          properties:
                            podTemplate:
                              description: PodTemplate describes the pod specification
                                for the unit.
                              properties:
                                image:
                                  description: Image for the underlying pod
                                  type: string
                                resources:
                                  description: Resources is for configuring the compute
                                    resources required
                                  properties:
                                    limits:
                                      additionalProperties:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      description: 'Limits describes the maximum amount
                                        of compute resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                                      type: object
                                    requests:
                                      additionalProperties:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      description: 'Requests describes the minimum
                                        amount of compute resources required. If Requests
                                        is omitted for a container, it defaults to
                                        Limits if that is explicitly specified, otherwise
                                        to an implementation-defined value. More info:
                                        https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                                      type: object
                                  type: object
                                tolerations:
                                  description: Toleartions the underlying pods should
                                    have
                                  items:
                                    description: The pod this Toleration is attached
                                      to tolerates any taint that matches the triple
                                      <key,value,effect> using the matching operator
                                      <operator>.
                                    properties:
                                      effect:
                                        description: Effect indicates the taint effect
                                          to match. Empty means match all taint effects.
                                          When specified, allowed values are NoSchedule,
                                          PreferNoSchedule and NoExecute.
                                        type: string
                                      key:
                                        description: Key is the taint key that the
                                          toleration applies to. Empty means match
                                          all taint keys. If the key is empty, operator
                                          must be Exists; this combination means to
                                          match all values and all keys.
                                        type: string
                                      operator:
                                        description: Operator represents a key's relationship
                                          to the value. Valid operators are Exists
                                          and Equal. Defaults to Equal. Exists is
                                          equivalent to wildcard for value, so that
                                          a pod can tolerate all taints of a particular
                                          category.
                                        type: string
                                      tolerationSeconds:
                                        description: TolerationSeconds represents
                                          the period of time the toleration (which
                                          must be of effect NoExecute, otherwise this
                                          field is ignored) tolerates the taint. By
                                          default, it is not set, which means tolerate
                                          the taint forever (do not evict). Zero and
                                          negative values will be treated as 0 (evict
                                          immediately) by the system.
                                        format: int64
                                        type: integer
                                      value:
                                        description: Value is the taint value the
                                          toleration matches to. If the operator is
                                          Exists, the value should be empty, otherwise
                                          just a regular string.
                                        type: string
                                    type: object
                                  type: array
                              type: object
                          type: object
                        maxConcurrency:
                          description: MaxConcurrency is the maximum no, of batch
                            processors to run concurrently. This spec is useful when
                            the sink group pod operates in asynchronous mode. Loader
                            pods does not needed this as they are synchronous.
                          type: integer
                        maxProcessingTime:
                          description: MaxProcessingTime is the max time in ms required
                            to consume one message. Defaults for the batcher is 180000ms
                            and loader is 600000ms.
                          format: int32
                          type: integer
                        maxReloadingUnits:
                          description: MaxReloadingUnits is the maximum number of
                            units(pods) that can be launched based on the DeploymentUnit
                            specification. Only valid for Reloading SinkGroup.
                          format: int32
                          type: integer
                        maxSizePerBatch:
                          anyOf:
                          - type: integer
                          - type: string
                          description: 'MaxSizePerBatch is the maximum size of the
                            batch in bytes, Ki, Mi, Gi Example values: 1000, 1Ki,
                            100Mi, 1Gi 1000 is 1000 bytes, 1Ki is 1 Killo byte, 100Mi
                            is 100 mega bytes, 1Gi is 1 Giga bytes'
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        maxWaitSeconds:
                          description: MaxWaitSeconds is the maximum time to wait
                            before making a batch, make a batch if MaxSizePerBatch
                            is not hit during MaxWaitSeconds.
                          type: integer
                      type: object
                    reload:
                      description: Reload specifies the ReloadSinkGroup specification,
                        overwrites All
                      properties:
                        deploymentUnit:
                          description: 'DeploymentUnit(pod) is the unit of deployment
                            for the batcher or the loader. Using this user can specify
                            the amount of resources needed to run them as one unit.
                            Operator calculates the total units based on the total
                            number of topics and this unit spec. This majorly solves
                            the scaling issues described in #167.'
                          properties:
                            podTemplate:
                              description: PodTemplate describes the pod specification
                                for the unit.
                              properties:
                                image:
                                  description: Image for the underlying pod
                                  type: string
                                resources:
                                  description: Resources is for configuring the compute
                                    resources required
                                  properties:
                                    limits:
                                      additionalProperties:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      description: 'Limits describes the maximum amount
                                        of compute resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                                      type: object
                                    requests:
                                      additionalProperties:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      description: 'Requests describes the minimum
                                        amount of compute resources required. If Requests
                                        is omitted for a container, it defaults to
                                        Limits if that is explicitly specified, otherwise
                                        to an implementation-defined value. More info:
                                        https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                                      type: object
                                  type: object
                                tolerations:
                                  description: Toleartions the underlying pods should
                                    have
                                  items:
                                    description: The pod this Toleration is attached
                                      to tolerates any taint that matches the triple
                                      <key,value,effect> using the matching operator
                                      <operator>.
                                    properties:
                                      effect:
                                        description: Effect indicates the taint effect
                                          to match. Empty means match all taint effects.
                                          When specified, allowed values are NoSchedule,
                                          PreferNoSchedule and NoExecute.
                                        type: string
                                      key:
                                        description: Key is the taint key that the
                                          toleration applies to. Empty means match
                                          all taint keys. If the key is empty, operator
                                          must be Exists; this combination means to
                                          match all values and all keys.
                                        type: string
                                      operator:
                                        description: Operator represents a key's relationship
                                          to the value. Valid operators are Exists
                                          and Equal. Defaults to Equal. Exists is
                                          equivalent to wildcard for value, so that
                                          a pod can tolerate all taints of a particular
                                          category.
                                        type: string
                                      tolerationSeconds:
                                        description: TolerationSeconds represents
                                          the period of time the toleration (which
                                          must be of effect NoExecute, otherwise this
                                          field is ignored) tolerates the taint. By
                                          default, it is not set, which means tolerate
                                          the taint forever (do not evict). Zero and
                                          negative values will be treated as 0 (evict
                                          immediately) by the system.
                                        format: int64
                                        type: integer
                                      value:
                                        description: Value is the taint value the
                                          toleration matches to. If the operator is
                                          Exists, the value should be empty, otherwise
                                          just a regular string.
                                        type: string
                                    type: object
                                  type: array
                              type: object
                          type: object
                        maxConcurrency:
                          description: MaxConcurrency is the maximum no, of batch
                            processors to run concurrently. This spec is useful when
                            the sink group pod operates in asynchronous mode. Loader
                            pods does not needed this as they are synchronous.
                          type: integer
                        maxProcessingTime:
                          description: MaxProcessingTime is the max time in ms required
                            to consume one message. Defaults for the batcher is 180000ms
                            and loader is 600000ms.
                          format: int32
                          type: integer
                        maxReloadingUnits:
                          description: MaxReloadingUnits is the maximum number of
                            units(pods) that can be launched based on the DeploymentUnit
                            specification. Only valid for Reloading SinkGroup.
                          format: int32
                          type: integer
                        maxSizePerBatch:
                          anyOf:
                          - type: integer
                          - type: string
                          description: 'MaxSizePerBatch is the maximum size of the
                            batch in bytes, Ki, Mi, Gi Example values: 1000, 1Ki,
                            100Mi, 1Gi 1000 is 1000 bytes, 1Ki is 1 Killo byte, 100Mi
                            is 100 mega bytes, 1Gi is 1 Giga bytes'
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        maxWaitSeconds:
                          description: MaxWaitSeconds is the maximum time to wait
                            before making a batch, make a batch if MaxSizePerBatch
                            is not hit during MaxWaitSeconds.
                          type: integer
                      type: object
                    reloadDupe:
                      description: ReloadDupe specifies the ReloadDupeSinkGroup specification,
                        overwrites All
                      properties:
                        deploymentUnit:
                          description: 'DeploymentUnit(pod) is the unit of deployment
                            for the batcher or the loader. Using this user can specify
                            the amount of resources needed to run them as one unit.
                            Operator calculates the total units based on the total
                            number of topics and this unit spec. This majorly solves
                            the scaling issues described in #167.'
                          properties:
                            podTemplate:
                              description: PodTemplate describes the pod specification
                                for the unit.
                              properties:
                                image:
                                  description: Image for the underlying pod
                                  type: string
                                resources:
                                  description: Resources is for configuring the compute
                                    resources required
                                  properties:
                                    limits:
                                      additionalProperties:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      description: 'Limits describes the maximum amount
                                        of compute resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                                      type: object
                                    requests:
                                      additionalProperties:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      description: 'Requests describes the minimum
                                        amount of compute resources required. If Requests
                                        is omitted for a container, it defaults to
                                        Limits if that is explicitly specified, otherwise
                                        to an implementation-defined value. More info:
                                        https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                                      type: object
                                  type: object
                                tolerations:
                                  description: Toleartions the underlying pods should
                                    have
                                  items:
                                    description: The pod this Toleration is attached
                                      to tolerates any taint that matches the triple
                                      <key,value,effect> using the matching operator
                                      <operator>.
                                    properties:
                                      effect:
                                        description: Effect indicates the taint effect
                                          to match. Empty means match all taint effects.
                                          When specified, allowed values are NoSchedule,
                                          PreferNoSchedule and NoExecute.
                                        type: string
                                      key:
                                        description: Key is the taint key that the
                                          toleration applies to. Empty means match
                                          all taint keys. If the key is empty, operator
                                          must be Exists; this combination means to
                                          match all values and all keys.
                                        type: string
                                      operator:
                                        description: Operator represents a key's relationship
                                          to the value. Valid operators are Exists
                                          and Equal. Defaults to Equal. Exists is
                                          equivalent to wildcard for value, so that
                                          a pod can tolerate all taints of a particular
                                          category.
                                        type: string
                                      tolerationSeconds:
                                        description: TolerationSeconds represents
                                          the period of time the toleration (which
                                          must be of effect NoExecute, otherwise this
                                          field is ignored) tolerates the taint. By
                                          default, it is not set, which means tolerate
                                          the taint forever (do not evict). Zero and
                                          negative values will be treated as 0 (evict
                                          immediately) by the system.
                                        format: int64
                                        type: integer
                                      value:
                                        description: Value is the taint value the
                                          toleration matches to. If the operator is
                                          Exists, the value should be empty, otherwise
                                          just a regular string.
                                        type: string
                                    type: object
                                  type: array
                              type: object
                          type: object
                        maxConcurrency:
                          description: MaxConcurrency is the maximum no, of batch
                            processors to run concurrently. This spec is useful when
                            the sink group pod operates in asynchronous mode. Loader
                            pods does not needed this as they are synchronous.
                          type: integer
                        maxProcessingTime:
                          description: MaxProcessingTime is the max time in ms required
                            to consume one message. Defaults for the batcher is 180000ms
                            and loader is 600000ms.
                          format: int32
                          type: integer
                        maxReloadingUnits:
                          description: MaxReloadingUnits is the maximum number of
                            units(pods) that can be launched based on the DeploymentUnit
                            specification. Only valid for Reloading SinkGroup.
                          format: int32
                          type: integer
                        maxSizePerBatch:
                          anyOf:
                          - type: integer
                          - type: string
                          description: 'MaxSizePerBatch is the maximum size of the
                            batch in bytes, Ki, Mi, Gi Example values: 1000, 1Ki,
                            100Mi, 1Gi 1000 is 1000 bytes, 1Ki is 1 Killo byte, 100Mi
                            is 100 mega bytes, 1Gi is 1 Giga bytes'
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        maxWaitSeconds:
                          description: MaxWaitSeconds is the maximum time to wait
                            before making a batch, make a batch if MaxSizePerBatch
                            is not hit during MaxWaitSeconds.
                          type: integer
                      type: object
                  type: object
                suspend:
                  description: Supsend is used to suspend batcher pods. Defaults to
                    false.
                  type: boolean
              type: object
            kafkaBrokers:
              description: Kafka configurations like consumer group and topics to
                watch
              type: string
            kafkaLoaderTopicPrefix:
              type: string
            kafkaTopicRegexes:
              type: string
            kafkaVersion:
              type: string
            loader:
              description: RedshiftLoaderSpec defines the desired state of RedshifLoader
              properties:
                maxProcessingTime:
                  description: MaxProcessingTime is the sarama configuration MaxProcessingTime
                    It is the max time in milliseconds required to consume one message.
                    Defaults to 600000ms (10mins)
                  format: int32
                  type: integer
                maxSize:
                  description: 'Deprecated all of the below spec in favour of SinkGroup
                    #167 Max configurations for the loader to batch the load'
                  type: integer
                maxWaitSeconds:
                  type: integer
                podTemplate:
                  description: PodTemplate describes the pods that will be created.
                    if this is not specifed, a default pod template is created
                  properties:
                    image:
                      description: Image for the underlying pod
                      type: string
                    resources:
                      description: Resources is for configuring the compute resources
                        required
                      properties:
                        limits:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: 'Limits describes the maximum amount of compute
                            resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                          type: object
                        requests:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: 'Requests describes the minimum amount of compute
                            resources required. If Requests is omitted for a container,
                            it defaults to Limits if that is explicitly specified,
                            otherwise to an implementation-defined value. More info:
                            https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                          type: object
                      type: object
                    tolerations:
                      description: Toleartions the underlying pods should have
                      items:
                        description: The pod this Toleration is attached to tolerates
                          any taint that matches the triple <key,value,effect> using
                          the matching operator <operator>.
                        properties:
                          effect:
                            description: Effect indicates the taint effect to match.
                              Empty means match all taint effects. When specified,
                              allowed values are NoSchedule, PreferNoSchedule and
                              NoExecute.
                            type: string
                          key:
                            description: Key is the taint key that the toleration
                              applies to. Empty means match all taint keys. If the
                              key is empty, operator must be Exists; this combination
                              means to match all values and all keys.
                            type: string
                          operator:
                            description: Operator represents a key's relationship
                              to the value. Valid operators are Exists and Equal.
                              Defaults to Equal. Exists is equivalent to wildcard
                              for value, so that a pod can tolerate all taints of
                              a particular category.
                            type: string
                          tolerationSeconds:
                            description: TolerationSeconds represents the period of
                              time the toleration (which must be of effect NoExecute,
                              otherwise this field is ignored) tolerates the taint.
                              By default, it is not set, which means tolerate the
                              taint forever (do not evict). Zero and negative values
                              will be treated as 0 (evict immediately) by the system.
                            format: int64
                            type: integer
                          value:
                            description: Value is the taint value the toleration matches
                              to. If the operator is Exists, the value should be empty,
                              otherwise just a regular string.
                            type: string
                        type: object
                      type: array
                  type: object
                redshiftGroup:
                  description: RedshiftGroup to give the access to when new topics
                    gets released
                  type: string
                redshiftMaxIdleConns:
                  description: RedshiftMaxIdleConns is the maximum idle connections
                    allowed
                  type: integer
                redshiftMaxOpenConns:
                  description: RedshiftMaxOpenConns is the maximum open connections
                    allowed
                  type: integer
                redshiftSchema:
                  description: RedshiftSchema to sink the data in
                  type: string
                sinkGroup:
                  description: SinkGroup contains the specification for main, reload
                    and reloadDupe sinkgroups. Operator uses 3 groups to perform Redshiftsink.
                    The topics which have never been released is part of Reload SinkGroup,
                    the topics which gets released moves to the Main SinkGroup. ReloadDupe
                    SinkGroup is used to give realtime upaates to the topics which
                    are reloading. Defaults are there for all sinkGroups if none is
                    specifed.
                  properties:
                    all:
                      description: All specifies a common specification for all SinkGroups
                      properties:
                        deploymentUnit:
                          description: 'DeploymentUnit(pod) is the unit of deployment
                            for the batcher or the loader. Using this user can specify
                            the amount of resources needed to run them as one unit.
                            Operator calculates the total units based on the total
                            number of topics and this unit spec. This majorly solves
                            the scaling issues described in #167.'
                          properties:
                            podTemplate:
                              description: PodTemplate describes the pod specification
                                for the unit.
                              properties:
                                image:
                                  description: Image for the underlying pod
                                  type: string
                                resources:
                                  description: Resources is for configuring the compute
                                    resources required
                                  properties:
                                    limits:
                                      additionalProperties:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      description: 'Limits describes the maximum amount
                                        of compute resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                                      type: object
                                    requests:
                                      additionalProperties:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      description: 'Requests describes the minimum
                                        amount of compute resources required. If Requests
                                        is omitted for a container, it defaults to
                                        Limits if that is explicitly specified, otherwise
                                        to an implementation-defined value. More info:
                                        https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                                      type: object
                                  type: object
                                tolerations:
                                  description: Toleartions the underlying pods should
                                    have
                                  items:
                                    description: The pod this Toleration is attached
                                      to tolerates any taint that matches the triple
                                      <key,value,effect> using the matching operator
                                      <operator>.
                                    properties:
                                      effect:
                                        description: Effect indicates the taint effect
                                          to match. Empty means match all taint effects.
                                          When specified, allowed values are NoSchedule,
                                          PreferNoSchedule and NoExecute.
                                        type: string
                                      key:
                                        description: Key is the taint key that the
                                          toleration applies to. Empty means match
                                          all taint keys. If the key is empty, operator
                                          must be Exists; this combination means to
                                          match all values and all keys.
                                        type: string
                                      operator:
                                        description: Operator represents a key's relationship
                                          to the value. Valid operators are Exists
                                          and Equal. Defaults to Equal. Exists is
                                          equivalent to wildcard for value, so that
                                          a pod can tolerate all taints of a particular
                                          category.
                                        type: string
                                      tolerationSeconds:
                                        description: TolerationSeconds represents
                                          the period of time the toleration (which
                                          must be of effect NoExecute, otherwise this
                                          field is ignored) tolerates the taint. By
                                          default, it is not set, which means tolerate
                                          the taint forever (do not evict). Zero and
                                          negative values will be treated as 0 (evict
                                          immediately) by the system.
                                        format: int64
                                        type: integer
                                      value:
                                        description: Value is the taint value the
                                          toleration matches to. If the operator is
                                          Exists, the value should be empty, otherwise
                                          just a regular string.
                                        type: string
                                    type: object
                                  type: array
                              type: object
                          type: object
                        maxConcurrency:
                          description: MaxConcurrency is the maximum no, of batch
                            processors to run concurrently. This spec is useful when
                            the sink group pod operates in asynchronous mode. Loader
                            pods does not needed this as they are synchronous.
                          type: integer
                        maxProcessingTime:
                          description: MaxProcessingTime is the max time in ms required
                            to consume one message. Defaults for the batcher is 180000ms
                            and loader is 600000ms.
                          format: int32
                          type: integer
                        maxReloadingUnits:
                          description: MaxReloadingUnits is the maximum number of
                            units(pods) that can be launched based on the DeploymentUnit
                            specification. Only valid for Reloading SinkGroup.
                          format: int32
                          type: integer
                        maxSizePerBatch:
                          anyOf:
                          - type: integer
                          - type: string
                          description: 'MaxSizePerBatch is the maximum size of the
                            batch in bytes, Ki, Mi, Gi Example values: 1000, 1Ki,
                            100Mi, 1Gi 1000 is 1000 bytes, 1Ki is 1 Killo byte, 100Mi
                            is 100 mega bytes, 1Gi is 1 Giga bytes'
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        maxWaitSeconds:
                          description: MaxWaitSeconds is the maximum time to wait
                            before making a batch, make a batch if MaxSizePerBatch
                            is not hit during MaxWaitSeconds.
                          type: integer
                      type: object
                    main:
                      description: Main specifies the MainSinkGroup specification,
                        overwrites All
                      properties:
                        deploymentUnit:
                          description: 'DeploymentUnit(pod) is the unit of deployment
                            for the batcher or the loader. Using this user can specify
                            the amount of resources needed to run them as one unit.
                            Operator calculates the total units based on the total
                            number of topics and this unit spec. This majorly solves
                            the scaling issues described in #167.'
                          properties:
                            podTemplate:
                              description: PodTemplate describes the pod specification
                                for the unit.
                              properties:
                                image:
                                  description: Image for the underlying pod
                                  type: string
                                resources:
                                  description: Resources is for configuring the compute
                                    resources required
                                  properties:
                                    limits:
                                      additionalProperties:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      description: 'Limits describes the maximum amount
                                        of compute resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                                      type: object
                                    requests:
                                      additionalProperties:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      description: 'Requests describes the minimum
                                        amount of compute resources required. If Requests
                                        is omitted for a container, it defaults to
                                        Limits if that is explicitly specified, otherwise
                                        to an implementation-defined value. More info:
                                        https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                                      type: object
                                  type: object
                                tolerations:
                                  description: Toleartions the underlying pods should
                                    have
                                  items:
                                    description: The pod this Toleration is attached
                                      to tolerates any taint that matches the triple
                                      <key,value,effect> using the matching operator
                                      <operator>.
                                    properties:
                                      effect:
                                        description: Effect indicates the taint effect
                                          to match. Empty means match all taint effects.
                                          When specified, allowed values are NoSchedule,
                                          PreferNoSchedule and NoExecute.
                                        type: string
                                      key:
                                        description: Key is the taint key that the
                                          toleration applies to. Empty means match
                                          all taint keys. If the key is empty, operator
                                          must be Exists; this combination means to
                                          match all values and all keys.
                                        type: string
                                      operator:
                                        description: Operator represents a key's relationship
                                          to the value. Valid operators are Exists
                                          and Equal. Defaults to Equal. Exists is
                                          equivalent to wildcard for value, so that
                                          a pod can tolerate all taints of a particular
                                          category.
                                        type: string
                                      tolerationSeconds:
                                        description: TolerationSeconds represents
                                          the period of time the toleration (which
                                          must be of effect NoExecute, otherwise this
                                          field is ignored) tolerates the taint. By
                                          default, it is not set, which means tolerate
                                          the taint forever (do not evict). Zero and
                                          negative values will be treated as 0 (evict
                                          immediately) by the system.
                                        format: int64
                                        type: integer
                                      value:
                                        description: Value is the taint value the
                                          toleration matches to. If the operator is
                                          Exists, the value should be empty, otherwise
                                          just a regular string.
                                        type: string
                                    type: object
                                  type: array
                              type: object
                          type: object
                        maxConcurrency:
                          description: MaxConcurrency is the maximum no, of batch
                            processors to run concurrently. This spec is useful when
                            the sink group pod operates in asynchronous mode. Loader
                            pods does not needed this as they are synchronous.
                          type: integer
                        maxProcessingTime:
                          description: MaxProcessingTime is the max time in ms required
                            to consume one message. Defaults for the batcher is 180000ms
                            and loader is 600000ms.
                          format: int32
                          type: integer
                        maxReloadingUnits:
                          description: MaxReloadingUnits is the maximum number of
                            units(pods) that can be launched based on the DeploymentUnit
                            specification. Only valid for Reloading SinkGroup.
                          format: int32
                          type: integer
                        maxSizePerBatch:
                          anyOf:
                          - type: integer
                          - type: string
                          description: 'MaxSizePerBatch is the maximum size of the
                            batch in bytes, Ki, Mi, Gi Example values: 1000, 1Ki,
                            100Mi, 1Gi 1000 is 1000 bytes, 1Ki is 1 Killo byte, 100Mi
                            is 100 mega bytes, 1Gi is 1 Giga bytes'
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        maxWaitSeconds:
                          description: MaxWaitSeconds is the maximum time to wait
                            before making a batch, make a batch if MaxSizePerBatch
                            is not hit during MaxWaitSeconds.
                          type: integer
                      type: object
                    reload:
                      description: Reload specifies the ReloadSinkGroup specification,
                        overwrites All
                      properties:
                        deploymentUnit:
                          description: 'DeploymentUnit(pod) is the unit of deployment
                            for the batcher or the loader. Using this user can specify
                            the amount of resources needed to run them as one unit.
                            Operator calculates the total units based on the total
                            number of topics and this unit spec. This majorly solves
                            the scaling issues described in #167.'
                          properties:
                            podTemplate:
                              description: PodTemplate describes the pod specification
                                for the unit.
                              properties:
                                image:
                                  description: Image for the underlying pod
                                  type: string
                                resources:
                                  description: Resources is for configuring the compute
                                    resources required
                                  properties:
                                    limits:
                                      additionalProperties:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      description: 'Limits describes the maximum amount
                                        of compute resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                                      type: object
                                    requests:
                                      additionalProperties:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      description: 'Requests describes the minimum
                                        amount of compute resources required. If Requests
                                        is omitted for a container, it defaults to
                                        Limits if that is explicitly specified, otherwise
                                        to an implementation-defined value. More info:
                                        https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                                      type: object
                                  type: object
                                tolerations:
                                  description: Toleartions the underlying pods should
                                    have
                                  items:
                                    description: The pod this Toleration is attached
                                      to tolerates any taint that matches the triple
                                      <key,value,effect> using the matching operator
                                      <operator>.
                                    properties:
                                      effect:
                                        description: Effect indicates the taint effect
                                          to match. Empty means match all taint effects.
                                          When specified, allowed values are NoSchedule,
                                          PreferNoSchedule and NoExecute.
                                        type: string
                                      key:
                                        description: Key is the taint key that the
                                          toleration applies to. Empty means match
                                          all taint keys. If the key is empty, operator
                                          must be Exists; this combination means to
                                          match all values and all keys.
                                        type: string
                                      operator:
                                        description: Operator represents a key's relationship
                                          to the value. Valid operators are Exists
                                          and Equal. Defaults to Equal. Exists is
                                          equivalent to wildcard for value, so that
                                          a pod can tolerate all taints of a particular
                                          category.
                                        type: string
                                      tolerationSeconds:
                                        description: TolerationSeconds represents
                                          the period of time the toleration (which
                                          must be of effect NoExecute, otherwise this
                                          field is ignored) tolerates the taint. By
                                          default, it is not set, which means tolerate
                                          the taint forever (do not evict). Zero and
                                          negative values will be treated as 0 (evict
                                          immediately) by the system.
                                        format: int64
                                        type: integer
                                      value:
                                        description: Value is the taint value the
                                          toleration matches to. If the operator is
                                          Exists, the value should be empty, otherwise
                                          just a regular string.
                                        type: string
                                    type: object
                                  type: array
                              type: object
                          type: object
                        maxConcurrency:
                          description: MaxConcurrency is the maximum no, of batch
                            processors to run concurrently. This spec is useful when
                            the sink group pod operates in asynchronous mode. Loader
                            pods does not needed this as they are synchronous.
                          type: integer
                        maxProcessingTime:
                          description: MaxProcessingTime is the max time in ms required
                            to consume one message. Defaults for the batcher is 180000ms
                            and loader is 600000ms.
                          format: int32
                          type: integer
                        maxReloadingUnits:
                          description: MaxReloadingUnits is the maximum number of
                            units(pods) that can be launched based on the DeploymentUnit
                            specification. Only valid for Reloading SinkGroup.
                          format: int32
                          type: integer
                        maxSizePerBatch:
                          anyOf:
                          - type: integer
                          - type: string
                          description: 'MaxSizePerBatch is the maximum size of the
                            batch in bytes, Ki, Mi, Gi Example values: 1000, 1Ki,
                            100Mi, 1Gi 1000 is 1000 bytes, 1Ki is 1 Killo byte, 100Mi
                            is 100 mega bytes, 1Gi is 1 Giga bytes'
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        maxWaitSeconds:
                          description: MaxWaitSeconds is the maximum time to wait
                            before making a batch, make a batch if MaxSizePerBatch
                            is not hit during MaxWaitSeconds.
                          type: integer
                      type: object
                    reloadDupe:
                      description: ReloadDupe specifies the ReloadDupeSinkGroup specification,
                        overwrites All
                      properties:
                        deploymentUnit:
                          description: 'DeploymentUnit(pod) is the unit of deployment
                            for the batcher or the loader. Using this user can specify
                            the amount of resources needed to run them as one unit.
                            Operator calculates the total units based on the total
                            number of topics and this unit spec. This majorly solves
                            the scaling issues described in #167.'
                          properties:
                            podTemplate:
                              description: PodTemplate describes the pod specification
                                for the unit.
                              properties:
                                image:
                                  description: Image for the underlying pod
                                  type: string
                                resources:
                                  description: Resources is for configuring the compute
                                    resources required
                                  properties:
                                    limits:
                                      additionalProperties:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      description: 'Limits describes the maximum amount
                                        of compute resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                                      type: object
                                    requests:
                                      additionalProperties:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      description: 'Requests describes the minimum
                                        amount of compute resources required. If Requests
                                        is omitted for a container, it defaults to
                                        Limits if that is explicitly specified, otherwise
                                        to an implementation-defined value. More info:
                                        https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                                      type: object
                                  type: object
                                tolerations:
                                  description: Toleartions the underlying pods should
                                    have
                                  items:
                                    description: The pod this Toleration is attached
                                      to tolerates any taint that matches the triple
                                      <key,value,effect> using the matching operator
                                      <operator>.
                                    properties:
                                      effect:
                                        description: Effect indicates the taint effect
                                          to match. Empty means match all taint effects.
                                          When specified, allowed values are NoSchedule,
                                          PreferNoSchedule and NoExecute.
                                        type: string
                                      key:
                                        description: Key is the taint key that the
                                          toleration applies to. Empty means match
                                          all taint keys. If the key is empty, operator
                                          must be Exists; this combination means to
                                          match all values and all keys.
                                        type: string
                                      operator:
                                        description: Operator represents a key's relationship
                                          to the value. Valid operators are Exists
                                          and Equal. Defaults to Equal. Exists is
                                          equivalent to wildcard for value, so that
                                          a pod can tolerate all taints of a particular
                                          category.
                                        type: string
                                      tolerationSeconds:
                                        description: TolerationSeconds represents
                                          the period of time the toleration (which
                                          must be of effect NoExecute, otherwise this
                                          field is ignored) tolerates the taint. By
                                          default, it is not set, which means tolerate
                                          the taint forever (do not evict). Zero and
                                          negative values will be treated as 0 (evict
                                          immediately) by the system.
                                        format: int64
                                        type: integer
                                      value:
                                        description: Value is the taint value the
                                          toleration matches to. If the operator is
                                          Exists, the value should be empty, otherwise
                                          just a regular string.
                                        type: string
                                    type: object
                                  type: array
                              type: object
                          type: object
                        maxConcurrency:
                          description: MaxConcurrency is the maximum no, of batch
                            processors to run concurrently. This spec is useful when
                            the sink group pod operates in asynchronous mode. Loader
                            pods does not needed this as they are synchronous.
                          type: integer
                        maxProcessingTime:
                          description: MaxProcessingTime is the max time in ms required
                            to consume one message. Defaults for the batcher is 180000ms
                            and loader is 600000ms.
                          format: int32
                          type: integer
                        maxReloadingUnits:
                          description: MaxReloadingUnits is the maximum number of
                            units(pods) that can be launched based on the DeploymentUnit
                            specification. Only valid for Reloading SinkGroup.
                          format: int32
                          type: integer
                        maxSizePerBatch:
                          anyOf:
                          - type: integer
                          - type: string
                          description: 'MaxSizePerBatch is the maximum size of the
                            batch in bytes, Ki, Mi, Gi Example values: 1000, 1Ki,
                            100Mi, 1Gi 1000 is 1000 bytes, 1Ki is 1 Killo byte, 100Mi
                            is 100 mega bytes, 1Gi is 1 Giga bytes'
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        maxWaitSeconds:
                          description: MaxWaitSeconds is the maximum time to wait
                            before making a batch, make a batch if MaxSizePerBatch
                            is not hit during MaxWaitSeconds.
                          type: integer
                      type: object
                  type: object
                suspend:
                  description: 'Supsend when turned on makes sure no batcher pods
                    are running for this CRD object. Default: false'
                  type: boolean
              required:
              - redshiftGroup
              - redshiftSchema
              type: object
            releaseCondition:
              description: ReleaseCondition specifies the release condition to consider
                a topic realtime and to consider the topci to be moved from reloading
                to released This is relevant only if masking is turned on in mask
                configuration. It is used for live mask reloading.
              properties:
                maxBatcherLag:
                  description: MaxBatcherLag is the maximum lag the batcher consumer
                    group shoud have to be be considered to be operating in realtime
                    and to be considered for release.
                  format: int64
                  type: integer
                maxLoaderLag:
                  description: MaxLoaderLag is the maximum lag the loader consumer
                    group shoud have to be be considered to be operating in realtime
                    and to be considered for release.
                  format: int64
                  type: integer
              type: object
            secretRefName:
              description: 'Secrets to be used Default: the secret name and namespace
                provided in the controller flags'
              type: string
            secretRefNamespace:
              description: 'Secrets namespace to be used Default: the secret name
                and namespace provided in the controller flags'
              type: string
            topicReleaseCondition:
              additionalProperties:
                properties:
                  maxBatcherLag:
                    description: MaxBatcherLag is the maximum lag the batcher consumer
                      group shoud have to be be considered to be operating in realtime
                      and to be considered for release.
                    format: int64
                    type: integer
                  maxLoaderLag:
                    description: MaxLoaderLag is the maximum lag the loader consumer
                      group shoud have to be be considered to be operating in realtime
                      and to be considered for release.
                    format: int64
                    type: integer
                type: object
              description: TopicReleaseCondition is considered instead of ReleaseCondition
                if it is defined for a topic. This is used for topics which does not
                work well with central ReleaseCondition for all topics
              type: object
          required:
          - batcher
          - kafkaBrokers
          - kafkaTopicRegexes
          - loader
          type: object
        status:
          description: RedshiftSinkStatus defines the observed state of RedshiftSink
          properties:
            batcherReloadingTopics:
              description: BatcherReloadingTopics stores the list of topics which
                are currently reloading for the batcher deployments in the reload
                sink group. There is a limit to maximum topics that can be reloaded.
                (MaxReloadingUnits)
              items:
                type: string
              type: array
            loaderReloadingTopics:
              description: LoaderReloadingTopics stores the list of topics which are
                currently reloading for the loader deployments in the reload sink
                group. There is a limit to maximum topics that can be reloaded. (MaxReloadingUnits)
              items:
                type: string
              type: array
            maskStatus:
              description: MaskStatus stores the status of masking for topics if masking
                is enabled
              properties:
                currentMaskStatus:
                  additionalProperties:
                    description: TopicMaskStatus store the mask status of a single
                      topic
                    properties:
                      phase:
                        description: Phase determines the
                        type: string
                      releasedVersion:
                        description: ReleasedVersion is the last released version
                          for the topic
                        type: string
                      version:
                        description: MaskFileVersion is the current mask configuration
                          being worked on
                        type: string
                    type: object
                  description: CurrentMaskStatus stores the current status of mask
                    status of topics
                  type: object
                currentMaskVersion:
                  description: CurrentMaskVersion stores the mask version which was
                    completely rolled out in all the topics.
                  type: string
                desiredMaskStatus:
                  additionalProperties:
                    description: TopicMaskStatus store the mask status of a single
                      topic
                    properties:
                      phase:
                        description: Phase determines the
                        type: string
                      releasedVersion:
                        description: ReleasedVersion is the last released version
                          for the topic
                        type: string
                      version:
                        description: MaskFileVersion is the current mask configuration
                          being worked on
                        type: string
                    type: object
                  description: DesiredMaskStatus stores the current status of mask
                    status of topics
                  type: object
                desiredMaskedVersion:
                  description: DesiredMaskVersion stores the latest mask version which
                    should be completely rolled out in all the topics.
                  type: string
              type: object
            topicGroups:
              additionalProperties:
                properties:
                  currentOffset:
                    description: 'LoaderCurrentOffset stores the last read current
                      offset of the consumer group This is required to determine if
                      the consumer group has performed any processing in the past.
                      As for low throughput topics, the consumer group disappears
                      and distinguishing between never created and inactive consumer
                      groups become difficult. Which leads to low throughput consumer
                      groups not getting moved to realtime from reloading. TODO: This
                      is not dead field once a group moves to released and should
                      be cleaned after that(status needs to be updated)'
                    format: int64
                    type: integer
                  id:
                    description: ID stores the name of the consumer group for the
                      topic based on this batcher and loader consumer groups are made
                    type: string
                  loaderTopicPrefix:
                    description: LoaderTopicPrefix stores the name of the loader topic
                      prefix
                    type: string
                required:
                - id
                type: object
              description: TopicGroup stores the group info for the topic
              type: object
          type: object
      type: object
  version: v1
  versions:
  - name: v1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
