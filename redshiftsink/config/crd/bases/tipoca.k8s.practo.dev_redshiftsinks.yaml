
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: redshiftsinks.tipoca.k8s.practo.dev
spec:
  group: tipoca.k8s.practo.dev
  names:
    kind: RedshiftSink
    listKind: RedshiftSinkList
    plural: redshiftsinks
    singular: redshiftsink
  scope: Namespaced
  validation:
    openAPIV3Schema:
      description: RedshiftSink is the Schema for the redshiftsinks API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: RedshiftSinkSpec defines the desired state of RedshiftSink
          properties:
            batcher:
              description: 'INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                Important: Run "make" to regenerate code after modifying this file'
              properties:
                kafkaBrokers:
                  description: Kafka configurations like consumer group and topics
                    to watch
                  type: string
                kafkaGroup:
                  type: string
                kafkaLoaderTopicPrefix:
                  type: string
                kafkaTopicRegexes:
                  type: string
                mask:
                  description: 'Mask when turned on enables masking of the data Default:
                    false'
                  type: string
                maskConfigDir:
                  type: string
                maskConfigFileName:
                  type: string
                maxSize:
                  description: Max configurations for the batcher to batch
                  type: integer
                maxWaitSeconds:
                  type: integer
                secretRefName:
                  description: 'Secrets to be used by the batcher pod Default: the
                    secret name and namespace provided in the controller flags'
                  type: string
                secretRefNamespace:
                  type: string
                suspend:
                  description: 'Supsend when turned makes sure no batcher pods are
                    running for this CRD object. Default: false'
                  type: boolean
              required:
              - kafkaBrokers
              - kafkaGroup
              - kafkaTopicRegexes
              - maxSize
              - maxWaitSeconds
              - secretRefName
              - secretRefNamespace
              type: object
          required:
          - batcher
          type: object
        status:
          description: RedshiftSinkStatus defines the observed state of RedshiftSink
          type: object
      type: object
  version: v1
  versions:
  - name: v1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
